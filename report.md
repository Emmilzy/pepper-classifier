# Project Report — Pepper Leaf Disease Classifier

**Dataset Source:** The model was trained on the publicly available
PlantVillage dataset (Kaggle), using only the two bell pepper classes —
healthy and bacterial spot — to form a binary classification task.

**How to Use:** Users open the deployed Streamlit app, upload a photo of a
pepper leaf, and click "Classify." The app returns the predicted class
(Healthy or Bacterial Spot) with a confidence score.

**Challenges & Solutions:** [Fill in your group's actual experience, e.g.:
class imbalance in the dataset was addressed with data augmentation;
initial overfitting was reduced using dropout and a frozen base model
before fine-tuning; large model file size complicated GitHub uploads and
was resolved using Git LFS / an external hosting link.]

**Possible Improvements:** Expanding the dataset, testing additional
architectures (e.g., EfficientNet), and adding Grad-CAM visualizations to
explain predictions.

*(~120 words — edit the bracketed section with your group's real
challenges before submission.)*
